#include <time.h>
#include <stdio.h>
#include <stdlib.h> // sempre inclua para rand() e srand()

int main() {
    int numJogador, numComputador, result = 0;
    char tipoComparacao;

    // Gerar número aleatório
    srand(time(0));
    numComputador = rand() % 100 + 1; // Número entre 1 e 100

    // Início do jogo
    printf("**Bem-vindo ao Game: Maior, Menor, Igual!!**\n");
    printf("Você precisa escolher um número e o tipo de comparação.\n");
    printf("M. Maior\n");
    printf("N. Menor\n");
    printf("I. Igual\n\n");

    printf("Escolha a comparação: ");
    scanf(" %c", &tipoComparacao);

    printf("Digite um número de 1 a 100: ");
    scanf("%d", &numJogador);

    // Exibir número do computador
    printf("\nO número do computador é: %d\n", numComputador);

    switch (tipoComparacao) {
        case 'M':
        case 'm':
            printf("Você escolheu a opção MAIOR!\n");
            result = numJogador > numComputador ? 1 : 0; 
            break;

        case 'N':
        case 'n':
            printf("Você escolheu a opção MENOR!\n");
            result = numJogador < numComputador ? 1 : 0;
            break;

        case 'I':
        case 'i':
            printf("Você escolheu a opção IGUAL!\n");
            result = numJogador == numComputador ? 1 : 0;
            break;

        default:
            printf("Opção inválida! 😕\n");
            return 0;
    }

    printf("\nSeu número: %d | Computador: %d\n", numJogador, numComputador);   

    if (result == 1) {
        printf("🎉 **Você VENCEU!** Parabéns!!! 🎉\n");
    } else {
        printf("💀 Você perdeu! Tente novamente!\n");
    }

    return 0;
}
